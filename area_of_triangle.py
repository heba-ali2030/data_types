# write a function def area_triangle(b,h) to compute the area of triangle : formula is area= s*b*h

def area_triangle(b,h):

    area= 0.5 * b * h
    
    print("the area of a triangle of base", b, "and height", h, "is", area)
   

area_triangle(3,5)
# area_triangle(6,9)
